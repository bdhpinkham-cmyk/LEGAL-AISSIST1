# backend/

The new FastAPI backend will be built here starting in Phase 2 (Grounding
Engine). Existing Python logic at the repo root (agents.py, ingestion.py,
llm_router.py, gemini_client.py, rag.py) will be evaluated for reuse when
this gets built out.
