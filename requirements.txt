# ---------------------------------------------------------------------------
# Pro Se Legal Intelligence — dependencies
# ---------------------------------------------------------------------------
# Core UI framework (desktop .exe + Android .apk via `flet build`).
flet==0.24.1

# HTTP client used for CourtListener, Tavily/DuckDuckGo, Deepgram, Whisper.
requests==2.32.3

# Cloud LLM SDKs (the router imports lazily; install only what you use).
anthropic==0.39.0
openai==1.54.4
# Unified Google GenAI SDK — supports BOTH Google AI Studio (API key) and
# Vertex AI (GCP project + Application Default Credentials). Preferred for Gemini.
google-genai>=1.0.0
# Legacy Gemini SDK — optional API-key-only fallback if google-genai is absent.
google-generativeai==0.8.3

# Evidence ingestion.
pypdf==5.1.0            # PDF text extraction
Pillow==11.0.0         # image EXIF / GPS metadata extraction
pydub==0.25.1          # audio/video chunking for large recordings (desktop only)
imageio-ffmpeg==0.5.1  # pip-installable static ffmpeg binary for pydub (no system install needed)

# Court-ready document export onto numbered pleading paper.
python-docx==1.1.2

# Autonomous Court Portal Agent (browser automation).
# After installing, run once:  playwright install chromium
playwright==1.48.0
